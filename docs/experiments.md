# Experiments

Configs live in `experiments/configs/` and include dense + MoE defaults for all baseline variants.

- Dense family uses `Qwen/Qwen3-8B`
- MoE family uses `allenai/OLMoE-1B-7B-0125-Instruct`

Batch mode is multi-seed and produces both individual-run and aggregate outputs.
